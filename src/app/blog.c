#include "routes.h"
#include <stdio.h>

char *prep_blog_posts(char *keyword) {
  char *html_posts;
  for (int i = 0; i < sizeof(blog_posts); i++) {
    printf("testitesti");
  }

  return html_posts;
}